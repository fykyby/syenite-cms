<!DOCTYPE html>
<html>
    <body>
        <table>
            {% for key, value in data %}
                <tr>
                    <td>{{ key }}:</td>
                    <td>{{ value }}</td>
                </tr>
            {% endfor %}
        </table>
    </body>
</html>
